import type { NextPage } from "next";
import { SiteHead } from "../components/SiteHead";

const Home: NextPage = () => {
    return (
        <div id="home">
            <SiteHead />
        </div>
    );
};

export default Home;
